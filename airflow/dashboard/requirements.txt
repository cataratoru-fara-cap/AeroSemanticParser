# Dashboard image only — deliberately NOT the Airflow requirements.
# The dashboard is a separate read-only process; keeping the two lists
# apart is what lets either image change without rebuilding the other.
streamlit>=1.44          # st.context.theme (theme-aware charts) landed in 1.44
plotly>=5.24
pymongo>=4.0,<5.0
